{
  "name": "tubes3-maxwin777",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && vite build --config vite.background.config.ts && vite build --config vite.content.config.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "tesseract.js": "^7.0.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
